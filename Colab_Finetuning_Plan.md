# Qwen3.5-0.8B UAV Swarm Fine-Tuning Plan (Google Colab Free)

This document outlines a structured, step-by-step plan to LoRA fine-tune the **Qwen3.5-0.8B** Small Language Model (SLM) using the `training_traces.jsonl` dataset. By utilizing techniques like 4-bit quantization (via Unsloth), this process fits comfortably within the 15GB VRAM limits of the free Google Colab T4 GPU.

## 📥 Required Inputs from the `uav_swarm` Project

Before starting the Colab notebook, you will need to upload/import the following assets from this repository:

1. **`training_traces.jsonl`**: The complete dataset containing the 390 expert traces generated by the Gemma teacher model.
2. **The Exact System Prompt**: Located in `agent.py`. The prompt structure (the textual representation of $C^*$, $\tilde{E}$, $\tilde{K}$, and collisions) must match **exactly** during fine-tuning so the Qwen model recognizes the environment during live inference.
3. **The Target Schema**: The exact JSON format the system expects: `{"fn": "...", "args": {...}, "why": "..."}`.

---

## 🛠️ Phase 1: Environment & Dataset Setup

### 1. Provision the Colab Environment
- Start a new Google Colab notebook.
- Go to **Runtime > Change runtime type** and select **T4 GPU**.
- Install the required high-performance training libraries:
  ```python
  !pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
  !pip install --no-deps xformers trl peft accelerate bitsandbytes
  ```

### 2. Dataset Formatting (ChatML)
Qwen models are highly optimized for the ChatML format. You will need a python script in your Colab cell to read `training_traces.jsonl` and convert each line into a conversation array:
- **System**: The static system instructions (UAV swarm rules).
- **User**: The specific telemetry metrics for that iteration.
- **Assistant**: The exact JSON `decision` from the file.

*Split the data*: Keep 90% (350 traces) for training, and 10% (40 traces) for validation to ensure the model isn't overfitting.

---

## 🧠 Phase 2: Model Loading & LoRA Configuration

### 1. Load the Base Model in 4-bit
Using `Unsloth`, instantiate the `Qwen/Qwen3.5-0.8B-Instruct` model in 4-bit quantized mode. This compresses the model footprint significantly, leaving plenty of VRAM for the training gradients.

### 2. Apply LoRA (Low-Rank Adaptation)
We don't need to retrain the entire 800-million parameter model. We only need to teach it our specific JSON grammar and physics thresholds. 
- **Target Modules**: `q_proj`, `k_proj`, `v_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj`.
- **Rank ($r$)**: `16` (Provides a good balance of capability vs memory).
- **Alpha ($\alpha$)**: `32`.

---

## 🏋️ Phase 3: Supervised Fine-Tuning (SFT)

### 1. Configure the Trainer
Use the `SFTTrainer` from the HuggingFace `trl` library.
- **Batch Size**: 2 or 4 (with gradient accumulation steps to simulate a batch size of 16).
- **Learning Rate**: `2e-4` (Standard for LoRA).
- **Epochs**: `3` to `5`. Since the dataset is small (~350 rows), training will be extremely fast (likely under 10 minutes total).

### 2. Execute Training
Run the training loop and monitor the validation loss. You want to see the loss curve flatten out. If the validation loss starts increasing while training loss decreases, the model is overfitting and you should stop early.

---

## 📦 Phase 4: Export & Deployment

### 1. Merge and Export to GGUF
Once training is complete, the LoRA adapters must be merged back into the base model.
- Export the merged model directly to the **GGUF** format (`q4_k_m` or `q8_0` quantization) using Unsloth's built-in export tools. GGUF is the native format required by Ollama.

### 2. Import to Local Ollama
1. Download the newly created `qwen3.5-uav-swarm.gguf` file from Colab to your local machine.
2. Create an Ollama `Modelfile` on your local machine:
   ```dockerfile
   FROM ./qwen3.5-uav-swarm.gguf
   TEMPLATE """<|im_start|>system\n{{ .System }}<|im_end|>\n<|im_start|>user\n{{ .Prompt }}<|im_end|>\n<|im_start|>assistant\n"""
   PARAMETER temperature 0.1
   ```
3. Build the local model:
   ```bash
   ollama create uav-agent -f Modelfile
   ```

### 3. Run the Hardware-in-the-Loop Test
Switch `harness.py` back to `backend="ollama"`, ensure it points to your newly fine-tuned `uav-agent` model, and run the simulator to watch your offline Qwen model pilot the swarm flawlessly!
